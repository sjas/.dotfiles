#
# sjas .zsh_profile 
#

# Make zsh check its window size after a process completes
shopt -s checkwinsize


# SOURCE GLOBAL DEFINITIONS
    if [ -f /etc/zshrc ]; then
        . /etc/zshrc
    fi

    if [ -f ~/.zshrc ]; then
        . ~/.zshrc
    fi

